global:
  resolve_timeout: 5m

route:
  group_by: ['instance']
  group_wait: 30s
  group_interval: 5m
  repeat_interval: 1h
  receiver: 'null' # Ми поки поставимо null, щоб він не падав

receivers:
  - name: 'null'